/**
 *
 * @param code user typed code
 * @return suggested code by LLM
 */
export const getSuggestedCode = (code: string) => {
  return "suggested code";
};

/**
 *
 * @param code user submitted code
 * @returns code excution runtime
 */
export const getGreenCodeRuntime = (code: string) => {
  return 0;
};
