/**
 *
 * @param email skku email
 * @param studentId skku student id
 * @param password
 * @param passwordConfirm
 * @returns user info
 */
export const signup = (email: string, studentId: number, password: string) => {
  return "signup";
};
